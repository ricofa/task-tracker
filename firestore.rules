rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Learning-only rules for this no-auth Simple Task Tracker case study.
    match /tasks/{taskId} {
      allow read, write: if true;
    }
  }
}
